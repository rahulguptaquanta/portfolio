require('dotenv').config();const express=require('express');const cors=require('cors');const mongoose=require('mongoose');const path=require('path');const Project=require('./models/Project');const Contact=require('./models/Contact');
const app=express();app.use(cors());app.use(express.json());app.use(express.static(path.join(__dirname,'../frontend')));
app.get('/api/health',(req,res)=>res.json({status:'ok'}));app.use('/api/projects',require('./routes/projects'));
app.post('/api/contact',async(req,res)=>{try{const c=await Contact.create(req.body);res.status(201).json({message:'Message received',id:c._id})}catch(e){res.status(400).json({message:e.message})}});
app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'../frontend/index.html')));
const PORT=process.env.PORT||5000;mongoose.connect(process.env.MONGODB_URI||'mongodb://127.0.0.1:27017/rahul_portfolio').then(async()=>{if(await Project.countDocuments()===0)await Project.create({name:'ArtGallery Website',description:'Online platform for showcasing and purchasing artwork with artwork listings, seller upload, pricing, shopping cart, buying, payment page and user authentication.',technologies:['HTML','CSS','JavaScript','Node.js','Express.js','MongoDB','Mongoose','EJS'],github:'https://github.com/rahulguptaquanta',liveDemo:'#'});app.listen(PORT,()=>console.log(`Portfolio running at http://localhost:${PORT}`))}).catch(e=>{console.error('MongoDB connection failed:',e.message);app.listen(PORT,()=>console.log(`Portfolio running without DB at http://localhost:${PORT}`))});
